# Steps to Setting Up
1. In GCP, choose Cloud SQL and select Sandbox
2. Modify as needed 
3. Create instance
4. Create database within the instance and user if not one already
5. Test in Google Shell

## Trouble I came across
- It seems that it connect access the database. Tried to fix it and play around with the instance in GCP but didn't work. 
